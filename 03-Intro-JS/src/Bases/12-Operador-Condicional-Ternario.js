// Declaración de una variable booleana para representar el estado
const activo = true;

// Uso de un operador lógico para asignar un mensaje basado en el estado (Esto es un operador ternario)
const mensaje = activo && 'Activo';

// Imprime el mensaje en la consola
console.log(mensaje);
